#include <iostream>
using namespace std;
int a, n, x;
int cnt, deno;

void search( double p, double q , int x){
    if( x / deno  < 1 ||  ) return 0;
    if( x )
}

int main(){
    int p, q;
    while( cin >> p >> q >> a >> n && p ){
        cnt = 0;
        deno = 2;
    }
}