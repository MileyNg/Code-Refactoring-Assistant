#include<stdio.h>
#define Max1 100000
#define Max2 50000
main(){
  int i;
  int n, q;
  int S[Max], T[mAx];
  int left, right, mid, count = 0, key;

  scanf("%d", &n);
  for(i = 0; i < n; i++){
    scanf("%d", &S[i]);
  }
  scanf("%d", &q);
  for(i = 0; i < q; i++){
    scanf("%d", &T[i]);
  }
printf("%d\n", count);
return 0;

}