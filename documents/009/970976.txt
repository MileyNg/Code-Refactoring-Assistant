#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

double r,are,circ;
int main() {
cin >> r;
are = pi * r * r;
circ = 2 * pi * r;
cout << ficed << setprecision(6) << are << " " << circ << endl;
}