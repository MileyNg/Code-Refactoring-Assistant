#include<stdio.h>
#include<math.h>

int main(void)
{
  double a,b,c;

  c=M_PI*c/180

  printf("%f\n",(a*b*sin(c))/2);
  printf("%f\n",sqrt(a+b+a*a+b*b-2*a*b*cos(c)));
  printf("%f\n",b*cos(c));

  return 0;
}