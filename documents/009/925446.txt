#include<stdio.h>
int main(void)
{
   int x,y;
   while(1){
   scanf("%d%d",&x,&y);
   if(x <= y && 0 < x){
      printf("%d %d\n",x,y);
   }
   else if(y < x){
      printf("%d %d\n",y,x);
   }
   else if(x == 0 && y == 0) break;
   }
   return 0;
}