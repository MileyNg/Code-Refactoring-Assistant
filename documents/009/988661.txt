#include<stdio.h>

int main(void)
{

    int S;
    int h,m,s;
    
    scanf("%d",&S);
    if(S>=3600)
       for(h=0;S<3600;++h)};
    if else(S>=60)
       for(m=0;S<60;++m);
    else
        for(s=0;S=0;++s);
    printf("%d:%d:%d\n",h,m,s);     
    return 0;
}