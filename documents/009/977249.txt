#include <iostream>
using namespace std;
#define _ __LINE__
int main() {
    char h, e, l, o, sp, w, r, d;
    h=(_+_)*_; r=_; d=_;
    e=((_+_)*_)^_; l=_+e; o=(h/_)^e; sp=(_<<_)%h; w=e-_-_; r+=l; d+=_+w;
    cout << h << e << l << l << o << sp << w << o << r << l << d << endl;
}