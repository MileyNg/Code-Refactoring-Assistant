#include<stdio.h>
int main(void)
{
    int a,b;
    while(a !=0 && b != 0){
        
    scanf("%d %d",&a,&b);
    for(int H = 1; H<=a; H++){
        for(int W = 1; W<=b; W++){
            printf("#\n");
        }
    }
    }
    return 0;
}